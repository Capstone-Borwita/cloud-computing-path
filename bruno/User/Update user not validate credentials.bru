meta {
  name: Update user not validate credentials
  type: http
  seq: 8
}

put {
  url: {{base_url}}/api/{{api_version}}/users/
  body: json
  auth: bearer
}

auth:bearer {
  token: {{user_token}}
}

body:json {
  {
    "name": "User2002"
  }
}

body:multipart-form {
  email: user@mail.com
  password: 12345678
  name: user1
  token: {{user_token}}
}
